# Copy to .env.local and fill in. .env.local is gitignored.

# Resend API key — https://resend.com/api-keys
RESEND_API_KEY=

# Where enquiries are delivered.
CONTACT_TO_EMAIL=contact@srimithraconstruction.com

# Sender. Must be an address on a domain verified in Resend. Replies are routed
# to the enquirer, so this address does not need a mailbox behind it.
CONTACT_FROM_EMAIL="Srimithra Construction <noreply@srimithraconstruction.com>"

# Optional: the hero's secondary action plays this film when set.
NEXT_PUBLIC_HERO_VIDEO_URL=
